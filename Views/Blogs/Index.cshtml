@model IEnumerable<Feedback_Information.Models.Blog>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.blogdescrip)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.blogentrydate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.userid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.blogdescrip)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.blogentrydate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.userid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.blogid }) |
            @Html.ActionLink("Details", "Details", new { id=item.blogid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.blogid })
        </td>
    </tr>
}

</table>
